-- =============================================================
-- posts.place_label 추가 (C-2-3a)
--
-- 결정: 도시는 구조적 cities 엔티티로 만들지 않는다. 위치는 핀(location)+나라
-- (country_code) 필수 + 자유 지역명(place_label) 옵셔널로 남긴다. cities 테이블/
-- city_id는 유지하되(v1.1 부활용) 당장의 지역명 표시는 place_label로 해결한다.
-- 필요해지면 나중에 지오코딩 기반 정규화로 업그레이드할 수 있지만, cities 자체를
-- 지금 채우는 방향은 가지 않는다.
-- =============================================================

alter table posts add column place_label text;
