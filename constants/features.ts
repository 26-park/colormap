// 화면에서 기능을 켜고 끄는 플래그. **삭제 대신 숨기기**가 필요한 것만 여기 둔다
// (나중에 되살릴 계획이 확정된 것). 임시 실험용 토글을 쌓아두는 곳이 아니다.

/**
 * 나라상세·시군구상세의 "내 기록 / 모두" 세그먼트를 노출할지.
 *
 * false 면 세그먼트 UI 자체를 감춘다 — 탭이 하나만 남으면 누를 수도 없는 칩이
 * 덩그러니 남고, 두 화면 모두 그리드 위에 `기록 N` 라벨이 있어 무엇의 목록인지는
 * 전달되기 때문이다. `activeTab` 상태와 조회 로직('all' 분기 포함)은 **그대로
 * 남겨둔다** — 이 값을 true 로 되돌리면 예전 동작이 그대로 돌아온다.
 *
 * ⭐ 끈 이유: v1 에서는 남의 공개 글을 UI 에 노출하지 않기로 했다. **공개범위
 * 3단 체계와 RLS 는 손대지 않았다** — 나중에 탐색 기능을 만들 때 되살린다.
 *
 * ⚠️ 이걸 끄면서 **`can_view_post` RLS 가 화면을 통해 검증되던 유일한 지점이
 * 사라졌다.** 자세한 경위와 대안은 CLAUDE.md "권한/가시성 모델" 참고.
 */
export const SHOW_ALL_POSTS_TAB = false;
